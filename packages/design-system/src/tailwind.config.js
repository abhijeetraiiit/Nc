/**
 * Tailwind CSS Configuration for Neubrutalism Theme
 * Import this in your Next.js apps
 */

const { tailwindTheme } = require('./index');

module.exports = {
  theme: {
    extend: tailwindTheme,
  },
  plugins: [],
};
